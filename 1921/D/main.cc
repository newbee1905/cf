#include <iostream>
#include <vector>
#include <algorithm>

signed main() {
	std::ios_base::sync_with_stdio(false);
	std::cin.tie(nullptr);
	std::cout.tie(nullptr);

	int t;
	int m, n, sum = 0;

	for (std::cin >> t; t--; ) {
		std::cin >> n >> m;
	}
}
